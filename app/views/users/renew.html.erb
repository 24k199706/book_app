<h2>再登録画面</h2>
    <div class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                <h1>また会員になりますか？</h1>
                    <%= form_for :renew, url: reupdate_path do |f| %>
                    <label for="exampleFormControlInput1" class="form-label">メールアドレス</label>
                    <div class="mb-3">
                        <%= f.text_field :email ,class:'form-control' %>
                    </div>
                    <%= f.submit "再登録", class: "btn btn-primary"%>
                    <% end %>
            </div>
        </div>
    </div>